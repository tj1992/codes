#include<iostream>
#include<vector>
using namespace std;


struct tile
{
	vector<int>x;
};

int main()
{

	vector<tile>T;
	
	int n;
	cin>>n;
	T.reserve(n);
	for(int i = 0 ; i < n; i++)
	{
		vector<int>z;
		int l;
		while(cin>>l)
		{
			
			z.push_back(l);
		}
		
		cin.clear();
		T.push_back(z);
	}	
}
